# Producción. En el servidor podés usar .env o .env.production.
# cp .env.production.example .env

NODE_ENV=production

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=courses_platform

PORT=3010

JWT_SECRET=cambiar-por-un-secreto-fuerte-en-produccion
JWT_EXPIRES_IN=7d
